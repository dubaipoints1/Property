---
import BaseLayout from "~/layouts/BaseLayout.astro";
---

<BaseLayout
  title="Search | DubaiPoints"
  description="Search every UAE credit card review, salary-transfer offer, points-and-miles guide, and lifestyle deal on DubaiPoints."
  fullWidth
>
  <header class="dp-pagehead is-narrow">
    <div class="crumb">Search</div>
    <h1>What are you looking for?</h1>
    <p class="deck">Every card review, salary-transfer offer, guide, deal and news note currently included on DubaiPoints — searchable.</p>
  </header>

  <div class="dp-search-body">
    <div id="search" data-pagefind-ignore></div>

    <noscript>
      <p class="dp-search-fallback">
        Search requires JavaScript. The full site map is on the
        <a href="/sitemap-index.xml">sitemap</a>, or browse by section:
        <a href="/cards/">Cards</a> · <a href="/guides/">Guides</a> ·
        <a href="/news/">News</a> · <a href="/deals/">Deals</a> ·
        <a href="/airlines/">Airlines</a> · <a href="/banks/">Banks</a>.
      </p>
    </noscript>

    <p class="dp-search-tip">
      <strong>Tips.</strong>
      Search matches title, body and metadata. Use quotes to match a phrase
      (e.g. <code>"salary transfer"</code>).
    </p>
  </div>
</BaseLayout>

<link rel="stylesheet" href="/pagefind/pagefind-ui.css" />
<script src="/pagefind/pagefind-ui.js" is:inline></script>
<script is:inline>
  window.addEventListener("DOMContentLoaded", () => {
    if (typeof window.PagefindUI === "undefined") return;
    const ui = new window.PagefindUI({
      element: "#search",
      showImages: false,
      resetStyles: false,
      autofocus: true,
      showSubResults: true,
    });
    // Directory search forms submit here with ?q= (2026-09-06 audit, F-015).
    const q = new URLSearchParams(window.location.search).get("q");
    if (q && typeof ui.triggerSearch === "function") ui.triggerSearch(q);
    // Pagefind labels its input by title only (axe label-title-only).
    const input = document.querySelector(".pagefind-ui__search-input");
    if (input && !input.getAttribute("aria-label")) input.setAttribute("aria-label", "Search DubaiPoints");
  });
</script>

<style>
  .dp-search-body {
    max-width: 760px;
    margin: 0 auto;
    padding: 32px 32px 80px;
  }
  @media (max-width: 640px) {
    .dp-search-body { padding: 24px 16px 56px; }
  }
  #search { margin-bottom: 24px; }
  /* Override Pagefind UI defaults to match design idiom */
  /* Pagefind keeps its Clear button in the tab order while it is
     invisible (opacity 0) — a focus stop with no visible ring
     (2026-09-06 audit, F-025). */
  :global(.pagefind-ui__search-clear.pagefind-ui__suppressed) { display: none; }
  :global(.pagefind-ui) {
    --pagefind-ui-primary: var(--green);
    --pagefind-ui-text: var(--ink);
    --pagefind-ui-background: var(--paper);
    --pagefind-ui-border: var(--line);
    --pagefind-ui-tag: var(--shade-1);
    --pagefind-ui-border-width: 1px;
    --pagefind-ui-border-radius: 10px;
    --pagefind-ui-image-border-radius: 8px;
    --pagefind-ui-image-box-ratio: 3 / 2;
    --pagefind-ui-font: 'DM Sans', system-ui, sans-serif;
  }
  :global(.pagefind-ui__search-input) {
    font-family: 'DM Sans', system-ui, sans-serif;
    font-size: 16px !important;
  }
  .dp-search-fallback,
  .dp-search-tip {
    font-size: 13px;
    color: var(--ink-soft);
    line-height: 1.6;
    margin-top: 24px;
    padding: 16px 20px;
    background: var(--shade-1);
    border-radius: 10px;
  }
  .dp-search-tip strong { color: var(--ink); font-weight: 700; }
  .dp-search-tip code {
    background: var(--paper);
    padding: 1px 6px;
    border-radius: 4px;
    font-size: 12px;
    color: var(--ink);
    border: 1px solid var(--line);
  }
  .dp-search-fallback a,
  .dp-search-tip a {
    color: var(--green);
    font-weight: 600;
    text-decoration: underline;
    text-underline-offset: 2px;
  }
</style>
