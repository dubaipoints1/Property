---
import BaseLayout from "~/layouts/BaseLayout.astro";

// Internal style guide. Documents the .dp-* class system + CSS custom
// properties so future contributors and partner journalists have a single
// reference. Pagefind-ignored — this is a craft document, not editorial.

const palette = [
  { name: "--bg",       hex: "#fbfaf6", desc: "Page background — warm paper" },
  { name: "--paper",    hex: "#fdfcf8", desc: "Card / surface fill" },
  { name: "--shade-1",  hex: "#f1eee7", desc: "Soft tint band" },
  { name: "--shade-2",  hex: "#e0dccf", desc: "Stronger tint, hairlines" },
  { name: "--ink",      hex: "#1f2328", desc: "Primary text" },
  { name: "--ink-soft", hex: "#3d434d", desc: "Body text" },
  { name: "--muted",    hex: "#6b7280", desc: "Tertiary text, eyebrows" },
  { name: "--line",     hex: "#dad6cd", desc: "Border / divider" },
  { name: "--navy",     hex: "#1f3a4d", desc: "Hero band, accent dark" },
  { name: "--navy-deep",hex: "#0f1f2e", desc: "Footer, deepest tone" },
  { name: "--green",    hex: "#1f3a4d", desc: "Primary accent — navy after F.9. Name retained for migration; references --green throughout the codebase resolve to navy." },
  { name: "--gold",     hex: "#b8842a", desc: "Trust signal — Verified, asterisks" },
  { name: "--red",      hex: "#a30016", desc: "Affiliate asterisk, error" },
];

const typeScale = [
  { label: "Hero h1",        sample: "What's a UAE Mile actually worth?", style: "font-family:'Fraunces',serif;font-weight:500;font-size:clamp(32px,4.5vw,48px);letter-spacing:-1.2px;color:var(--ink);" },
  { label: "Section h2",     sample: "How the score works",               style: "font-family:'Fraunces',serif;font-weight:500;font-size:clamp(20px,2.6vw,26px);letter-spacing:-0.4px;color:var(--ink);" },
  { label: "Italic deck",    sample: "AED-first, source-verified, UAE-resident.", style: "font-family:'Fraunces',serif;font-style:italic;font-weight:500;font-size:22px;color:var(--ink-soft);" },
  { label: "Body p",         sample: "DubaiPoints publishes UAE-resident analysis.", style: "font-family:'DM Sans',sans-serif;font-size:15px;line-height:1.65;color:var(--ink-soft);" },
  { label: "Eyebrow",        sample: "EDITORIAL POLICY",                  style: "font-family:'DM Sans',sans-serif;font-size:11px;font-weight:700;letter-spacing:2.5px;text-transform:uppercase;color:var(--green);" },
  { label: "Tabular num",    sample: "AED 12,400.00",                     style: "font-family:'DM Sans',sans-serif;font-feature-settings:'tnum';font-size:15px;font-weight:600;color:var(--ink);" },
];

const components = [
  { name: ".dp-pagehead",            desc: "Section landing header. Wide variant for index pages." },
  { name: ".dp-pagehead.is-narrow",  desc: "Narrow gradient variant for article pages — used by ArticleLayout / TrustPageLayout." },
  { name: ".dp-byline-strip",        desc: "Avatar + filed/verified date strip. Used between pagehead and body on articles." },
  { name: ".dp-prose",               desc: "Editorial body wrapper. Drop-cap on first paragraph by default; opt-out via container modifier." },
  { name: ".dp-pullbox",             desc: "Highlighted info block inside prose. Green eyebrow + shaded background." },
  { name: ".dp-take",                desc: "\"Our take\" callout — green eyebrow + soft-tint background. Used on card reviews." },
  { name: ".dp-proscons",            desc: "Two-column pros/cons grid with green and red accents." },
  { name: ".dp-spec-card",           desc: "Sticky-sidebar spec card. is-call variant for the dark Editor's call block." },
  { name: ".dp-cr-mock",             desc: "Card review hero placeholder card art. Reads data-bank=\"<slug>\" for per-issuer tint." },
  { name: ".dp-verdict-pill",        desc: "Score pill on card review hero. Score / 5 + Last verified date." },
  { name: ".dp-tools-stats",         desc: "Inline stats strip below pagehead on tools pages." },
  { name: ".dp-tools-table",         desc: "Sortable / filterable tools table. Used on /salary-transfer/ and /valuations/." },
  { name: ".dp-tools-methodology",   desc: "3-column methodology footer for tools pages." },
  { name: ".dp-megapanel",           desc: "Desktop mega-menu panel. Shown on Cards / Airlines hover or focus." },
  { name: ".dp-feed",                desc: "Numbered feed list — used on section index pages below the featured grid." },
  { name: ".dp-story-card",          desc: "Hero / med / sm story card variants for the featured band." },
  { name: ".dp-trust-meta",          desc: "Compact 'Last updated · Editorial standards' strip below trust-page heads." },
  { name: ".dp-glossary",            desc: "<dl> styling for glossary terms — Fraunces dt, soft-ink dd, hairline rules." },
];
---

<BaseLayout
  title="Style guide — DubaiPoints"
  description="Internal reference for the DubaiPoints .dp-* class system, CSS custom properties, and editorial chrome. For contributors and partner journalists."
  fullWidth
>
  <div data-pagefind-ignore>
  <header class="dp-pagehead is-narrow">
    <div class="crumb">Internal · Style guide</div>
    <h1>The chrome, in one page.</h1>
    <p class="deck">Every CSS custom property, type scale, and `.dp-*` component used on the site, with examples. Maintained alongside the design handoff.</p>
  </header>

  <div class="sg-body">
    <section>
      <h2>Palette</h2>
      <p>Two-accent system. <strong>Green</strong> is the primary — links, eyebrows, success. <strong>Gold</strong> is the trust-signal accent — Verified chips, affiliate asterisks. Each colour has a single job.</p>
      <div class="sg-palette">
        {palette.map((c) => (
          <div class="sg-swatch">
            <div class="chip" style={`background: ${c.hex};`}></div>
            <div class="meta">
              <code>{c.name}</code>
              <span class="hex">{c.hex}</span>
              <span class="desc">{c.desc}</span>
            </div>
          </div>
        ))}
      </div>
    </section>

    <section>
      <h2>Type scale</h2>
      <p>Fraunces (serif) for headlines, eyebrows and the "Our take" labels. DM Sans for body and UI. Italic Fraunces 500 is reserved for the deck under article H1s.</p>
      <div class="sg-type">
        {typeScale.map((t) => (
          <div class="sg-type-row">
            <div class="lbl">{t.label}</div>
            <div class="sample" style={t.style}>{t.sample}</div>
          </div>
        ))}
      </div>
    </section>

    <section>
      <h2>Components</h2>
      <p>The full <code>.dp-*</code> namespace. Each component is documented in <code>src/styles/global.css</code> with section comments.</p>
      <div class="sg-components">
        {components.map((c) => (
          <div class="sg-component-row">
            <code>{c.name}</code>
            <span>{c.desc}</span>
          </div>
        ))}
      </div>
    </section>

    <section>
      <h2>Editorial guarantees</h2>
      <ul>
        <li><strong>AED-first.</strong> Every monetary figure on the site renders in dirhams unless explicitly noted. No USD defaults.</li>
        <li><strong>Source-verified.</strong> Every card review and salary-transfer offer carries a <em>Last verified</em> date. Pages with stamps older than 90 days are flagged in UI.</li>
        <li><strong>Affiliate asterisks.</strong> Any monetary link tied to an affiliate programme renders with a red asterisk and a footnote linking to <a href="/partnership/">/partnership/</a>. The asterisk is identical on cards we recommend and cards we don't.</li>
        <li><strong>No "Apply Now" CTAs.</strong> Buttons read "Read review", "See details", "Compare". The verdict drives the click, not the marketing.</li>
        <li><strong>HfP-dry voice.</strong> Evidence-led, AED-numerate, sceptical of programme marketing. Authority through specifics, not adjectives.</li>
      </ul>
    </section>

    <section>
      <h2>Voice fences (Standards Editor kill-list)</h2>
      <p>Every user-facing string is reviewed at Stage 6.5 of the workflow. A draft fails Standards review if any of the following appear. Each rule has an example of what we reject and what we ship instead.</p>
      <div class="sg-kill">
        <div class="sg-kill-row"><div class="r">1 · Snarky / Twitter-voice</div><div class="x">"No 'Apply Now' buttons because we're not a comparison engine."</div><div class="y">"We do not run affiliate buttons in editorial copy."</div></div>
        <div class="sg-kill-row"><div class="r">2 · Marketing hyperbole</div><div class="x">"The most trusted UAE points publication."</div><div class="y">"Independent. Source-verified. Reader-funded."</div></div>
        <div class="sg-kill-row"><div class="r">3 · Inside-baseball jargon</div><div class="x">"LCC loyalty done plainly."</div><div class="y">"Loyalty for Air Arabia's low-cost network."</div></div>
        <div class="sg-kill-row"><div class="r">4 · First-person plural in chrome</div><div class="x">"We think this card is good."</div><div class="y">"This card earns 4.2 of 5."</div></div>
        <div class="sg-kill-row"><div class="r">5 · Excess punctuation</div><div class="x">"Save with our newsletter!"</div><div class="y">"Subscribe to the Friday brief."</div></div>
        <div class="sg-kill-row"><div class="r">6 · Empty signposting</div><div class="x">"Here at DubaiPoints, we want to share something important: …"</div><div class="y">Just say the important thing.</div></div>
        <div class="sg-kill-row"><div class="r">7 · Print-publication metaphor</div><div class="x">"Prints the date next to every verdict."</div><div class="y">"Every verdict is dated."</div></div>
        <div class="sg-kill-row"><div class="r">8 · Inverted-pyramid violation</div><div class="x">Burying the most important word at the end of an h1.</div><div class="y">Lead with the most important word.</div></div>
        <div class="sg-kill-row"><div class="r">9 · Imperative bullying</div><div class="x">"Apply now / Don't miss out / Get yours."</div><div class="y">"Read review / See offers / Open the calculator."</div></div>
        <div class="sg-kill-row"><div class="r">10 · AED format drift</div><div class="x">"1,200 AED / Dh 1,200 / AED1200 / 5k+"</div><div class="y">"AED 1,200 / AED 5,000+"</div></div>
      </div>
    </section>

    <section>
      <h2>AED format rule</h2>
      <p>The standard is <code>AED&nbsp;&lt;number with thousand separators&gt;</code>. The space between unit and value is mandatory. Examples:</p>
      <ul>
        <li><strong>Allowed:</strong> <code>AED 1,200</code> · <code>AED 1,200 / yr</code> · <code>AED 5,000 – 8,000</code> · <code>AED 30,000+</code> · <code>AED 1,200/mo min</code></li>
        <li><strong>Forbidden:</strong> <code>1,200 AED</code> · <code>Dh 1,200</code> · <code>Dhs. 1,200</code> · <code>AED1200</code> · <code>AED 5k</code> · <code>AED 30k+</code></li>
      </ul>
      <p>Shorthand <code>k</code> / <code>k+</code> is reserved for prose where the number is descriptive (e.g. "salaries north of AED 30,000"); never use it for chrome strings, URL labels, or tracker copy.</p>
    </section>

    <section>
      <h2>Button labels</h2>
      <p>Buttons describe what the next surface is, not what the reader should do. Approved verbs:</p>
      <ul>
        <li><code>Read review</code> · <code>Read the brief</code> · <code>Read the {`{programme}`} guide</code></li>
        <li><code>See offers</code> · <code>See details</code> · <code>See methodology</code></li>
        <li><code>Open the calculator</code> · <code>Open the tracker</code></li>
        <li><code>Compare cards</code> · <code>Compare alternatives</code></li>
      </ul>
      <p>Forbidden: <code>Apply now</code>, <code>Sign up today</code>, <code>Don't miss out</code>, <code>Get yours</code>, <code>Click here</code>, <code>Read more →</code> (no destination).</p>
    </section>

    <section>
      <h2>Self-reference guard</h2>
      <p>Constructions that imply hyperbole or unverifiable rank are rejected even when they sound like brand voice:</p>
      <ul>
        <li><code>The only…</code> / <code>The leading…</code> / <code>The most trusted…</code></li>
        <li><code>The first publication to…</code> (only allowed when factually documented)</li>
        <li><code>Best-in-class</code> / <code>Industry-leading</code> / <code>Award-winning</code> (without the award)</li>
      </ul>
      <p>Verifiable substitutes: <code>Independent</code>, <code>Source-verified</code>, <code>Reader-funded</code>, <code>UAE-resident-led</code>.</p>
    </section>

    <section>
      <h2>Crumb format</h2>
      <p>Breadcrumb separator is <code>/</code> (forward slash with single spaces). Mid-string label separators are <code>·</code> (middle dot). Examples:</p>
      <ul>
        <li><code>DubaiPoints / Cards</code> (section index)</li>
        <li><code>Cards / Cashback / FAB</code> (card review)</li>
        <li><code>Tools / Salary transfer</code> (tools page)</li>
        <li><code>404 · Page not found</code> (status, not breadcrumb — uses ·)</li>
      </ul>
    </section>
  </div>
  </div>
</BaseLayout>

<style>
  .sg-body {
    max-width: 1080px;
    margin: 0 auto;
    padding: 32px 32px 80px;
  }
  section { margin-bottom: 48px; }
  section h2 {
    font-family: 'Fraunces', serif;
    font-weight: 500;
    font-size: clamp(22px, 2.6vw, 28px);
    letter-spacing: -0.5px;
    color: var(--ink);
    margin: 0 0 14px;
    padding-bottom: 12px;
    border-bottom: 1px solid var(--line);
  }
  section > p {
    font-size: 14px;
    color: var(--ink-soft);
    line-height: 1.6;
    margin: 0 0 20px;
    max-width: 70ch;
  }
  section > p code,
  .sg-component-row code {
    font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
    font-size: 12px;
    background: var(--shade-1);
    padding: 2px 6px;
    border-radius: 4px;
    color: var(--ink);
  }
  section ul { font-size: 14px; color: var(--ink-soft); line-height: 1.65; max-width: 70ch; }
  section ul li { margin-bottom: 8px; }

  .sg-palette {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 12px;
  }
  .sg-swatch {
    display: flex; gap: 12px;
    padding: 12px;
    background: var(--paper);
    border: 1px solid var(--line);
    border-radius: 10px;
  }
  .sg-swatch .chip {
    width: 40px; height: 40px;
    border-radius: 6px;
    border: 1px solid var(--line);
    flex-shrink: 0;
  }
  .sg-swatch .meta { display: grid; gap: 2px; min-width: 0; }
  .sg-swatch code {
    font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
    font-size: 12px;
    color: var(--ink);
    font-weight: 600;
  }
  .sg-swatch .hex { font-size: 11px; color: var(--muted); font-variant-numeric: tabular-nums; }
  .sg-swatch .desc { font-size: 11px; color: var(--ink-soft); line-height: 1.4; }

  .sg-type {
    display: grid; gap: 10px;
  }
  .sg-type-row {
    display: grid;
    grid-template-columns: 140px 1fr;
    gap: 16px;
    padding: 16px;
    background: var(--paper);
    border: 1px solid var(--line);
    border-radius: 10px;
    align-items: center;
  }
  .sg-type-row .lbl {
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    color: var(--muted);
  }

  .sg-components {
    display: grid; gap: 0;
    border: 1px solid var(--line);
    border-radius: 10px;
    overflow: hidden;
    background: var(--paper);
  }
  .sg-component-row {
    display: grid;
    grid-template-columns: 240px 1fr;
    gap: 16px;
    padding: 14px 18px;
    border-bottom: 1px solid var(--line-soft);
    font-size: 13px;
    color: var(--ink-soft);
    align-items: center;
  }
  .sg-component-row:last-child { border-bottom: 0; }
  .sg-component-row code { white-space: nowrap; }

  @media (max-width: 640px) {
    .sg-type-row,
    .sg-component-row { grid-template-columns: 1fr; gap: 6px; }
  }

  /* Voice fences kill-list */
  .sg-kill {
    display: grid;
    gap: 0;
    border: 1px solid var(--line);
    border-radius: 10px;
    overflow: hidden;
    background: var(--paper);
  }
  .sg-kill-row {
    display: grid;
    grid-template-columns: 200px 1fr 1fr;
    gap: 16px;
    padding: 14px 18px;
    border-bottom: 1px solid var(--line-soft);
    font-size: 13px;
    align-items: start;
  }
  .sg-kill-row:last-child { border-bottom: 0; }
  .sg-kill-row .r {
    font-family: 'DM Sans', system-ui, sans-serif;
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 1.8px;
    text-transform: uppercase;
    color: var(--muted);
  }
  .sg-kill-row .x {
    color: var(--ink-soft);
    text-decoration: line-through;
    text-decoration-color: var(--red);
    text-decoration-thickness: 1px;
  }
  .sg-kill-row .y {
    color: var(--ink);
    font-weight: 500;
    border-left: 2px solid var(--green);
    padding-left: 12px;
  }
  @media (max-width: 768px) {
    .sg-kill-row { grid-template-columns: 1fr; gap: 6px; }
    .sg-kill-row .y { border-left: 0; padding-left: 0; }
  }
</style>
