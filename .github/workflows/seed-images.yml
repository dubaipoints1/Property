# One-shot Pexels seed — Phase E.2 hands-off image fetch.
#
# Why this workflow exists:
#   The Claude sandbox is blocked from reaching api.pexels.com /
#   images.pexels.com / unsplash / pixabay / wikimedia at the network
#   allowlist layer. GitHub Actions runners are not, so the Pexels
#   fetcher script (scripts/images/fetch-stock.ts) runs cleanly here.
#
# How to use:
#   1. In the repo settings → Secrets and variables → Actions, add
#      a secret named PEXELS_API_KEY (free key from
#      https://www.pexels.com/api/).
#   2. Go to Actions → "seed-images" → Run workflow.
#   3. Pick the branch (e.g. claude/check-latest-updates-GkRze).
#   4. Click Run. The workflow fetches every seeded photo, writes
#      manifest entries, commits, and pushes to the same branch.
#
# What it fetches:
#   ~68 slugs covering every <StockImage> reference site-wide: homepage
#   hero, wallet, every active /cards/<slug>/ hero, every /banks/<slug>/
#   hero, every /guides/ hero, every /airlines/ programme hero, news,
#   deals. See scripts/images/fetch-stock.ts for the script the slugs
#   feed.
#
# Re-runnability (PR #167):
#   fetch-stock.ts is idempotent — existing manifest slugs no-op (log
#   SKIP and exit 0, no Pexels call). So the workflow is safe to re-run
#   any time. First seed-images run on a fresh repo fetches everything;
#   subsequent runs only fetch genuinely-new slugs. Pass --replace on a
#   per-step run override to force a refetch.
#
# Editorial guardrails:
#   - Pexels licence is "free to use, attribution not required" — we
#     attribute anyway via <ImageCredit>, per Charter §"No advertorial-
#     driven recommendations" (transparency on every surface).
#   - Per-slug query strings are conservative editor picks. If a
#     result looks wrong for its context, re-run with `--pick 2/3/4`
#     to grab a different result from the same search.
#   - Each fetch hits the Pexels API once; free tier is 200/hour, so
#     the ~68 calls in this workflow use ~34% of an hour's quota on the
#     first full seed.

name: seed-images

on:
  workflow_dispatch:
    inputs:
      branch:
        description: "Branch to seed (commits land here)"
        required: true
        default: "claude/check-latest-updates-GkRze"
        type: string
  # Push trigger — re-running this workflow on a checked-in branch when
  # the file changes lets the Chairman re-fetch without clicking through
  # the Actions UI. The workflow checks out the branch the push came in
  # on (github.ref_name) when no workflow_dispatch input is provided.
  # GITHUB_TOKEN-authored pushes never re-trigger by default, so the
  # workflow's own commit at the end won't cause a loop.
  push:
    branches:
      - "claude/**"
    paths:
      - ".github/workflows/seed-images.yml"

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout target branch
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.branch || github.ref_name }}
          fetch-depth: 1

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Fetch hero-cabin
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug hero-cabin \
            --query "business class window seat sunset" \
            --orientation landscape \
            --alt "Window seat in a premium cabin at sunset" \
            --usage "homepage hero"

      - name: Fetch wallet-hero-cards
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug wallet-hero-cards \
            --query "credit card stack marble desk" \
            --orientation landscape \
            --alt "Premium credit cards in a flat-lay" \
            --usage "homepage UAE Wallet"

      - name: Fetch card-fab-elite
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-fab-elite \
            --query "black credit card luxury close up" \
            --orientation landscape \
            --alt "Premium dark credit card on a textured surface"

      - name: Fetch card-emirates-nbd-etihad-guest-elevate
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-nbd-etihad-guest-elevate \
            --query "passport boarding pass airport" \
            --orientation landscape \
            --alt "Passport and boarding pass on a travel desk"

      - name: Fetch card-emirates-nbd-darna-select-visa
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-nbd-darna-select-visa \
            --query "Dubai apartment keys interior" \
            --orientation landscape \
            --alt "Modern Dubai apartment interior"

      - name: Fetch guide-expat-starter
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug guide-expat-starter \
            --query "Dubai skyline aerial" \
            --orientation landscape \
            --alt "Aerial view of the Dubai skyline"

      - name: Fetch guide-expat-starter-banking-basics
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug guide-expat-starter-banking-basics \
            --query "modern bank entrance" \
            --orientation landscape \
            --alt "Modern bank entrance with glass facade"

      - name: Fetch guide-expat-starter-first-credit-card
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug guide-expat-starter-first-credit-card \
            --query "hand holding credit card" \
            --orientation landscape \
            --alt "Hand holding a credit card"

      - name: Fetch guide-expat-starter-points-101
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug guide-expat-starter-points-101 \
            --query "boarding pass and passport on map" \
            --orientation landscape \
            --alt "Travel boarding pass laid across a world map"

      - name: Fetch guide-expat-starter-avoid-mistakes
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug guide-expat-starter-avoid-mistakes \
            --query "Dubai marina aerial" \
            --orientation landscape \
            --alt "Aerial view of Dubai Marina at golden hour"

      - name: Fetch guide-uae-credit-card-eligibility-2026
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug guide-uae-credit-card-eligibility-2026 \
            --query "paperwork laptop desk office" \
            --orientation landscape \
            --alt "Paperwork and laptop on an office desk"

      - name: Fetch programme-skywards
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug programme-skywards \
            --query "airbus a380 takeoff sunset" \
            --orientation landscape \
            --alt "Wide-body aircraft at sunset"

      - name: Fetch news-emirates-skywards-season-of-rewards-2026
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug news-emirates-skywards-season-of-rewards-2026 \
            --query "airport boarding pass summer travel" \
            --orientation landscape \
            --alt "Boarding pass and passport on a summer travel desk"

      - name: Fetch deal-enbd-skywards-welcome-q2-2026
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug deal-enbd-skywards-welcome-q2-2026 \
            --query "premium black credit card luxury desk" \
            --orientation landscape \
            --alt "Premium black credit card on a luxury desk"


      - name: Fetch card-sc-platinum-x
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-sc-platinum-x \
            --query "private banking office leather desk" \
            --orientation landscape \
            --alt "Private banking office interior"

      - name: Fetch card-citi-cashback
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-citi-cashback \
            --query "shopping bags receipt cashback" \
            --orientation landscape \
            --alt "Shopping bags and a receipt"

      - name: Fetch card-hsbc-live-plus
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-hsbc-live-plus \
            --query "shopping bags receipt cashback" \
            --orientation landscape \
            --alt "Shopping bags and a receipt"

      - name: Fetch card-cbd-one
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-cbd-one \
            --query "shopping bags receipt cashback" \
            --orientation landscape \
            --alt "Shopping bags and a receipt"

      - name: Fetch card-adib-cashback-visa
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-adib-cashback-visa \
            --query "modern minimal banking architecture" \
            --orientation landscape \
            --alt "Modern minimal banking architecture"

      - name: Fetch card-dib-consumer-cashback
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-dib-consumer-cashback \
            --query "modern minimal banking architecture" \
            --orientation landscape \
            --alt "Modern minimal banking architecture"

      - name: Fetch card-mashreq-cashback
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-mashreq-cashback \
            --query "shopping bags receipt cashback" \
            --orientation landscape \
            --alt "Shopping bags and a receipt"

      - name: Fetch card-rakbank-world
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-rakbank-world \
            --query "shopping bags receipt cashback" \
            --orientation landscape \
            --alt "Shopping bags and a receipt"

      - name: Fetch card-emirates-islamic-switch-cashback
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-islamic-switch-cashback \
            --query "modern minimal banking architecture" \
            --orientation landscape \
            --alt "Modern minimal banking architecture"

      - name: Fetch card-emirates-nbd-voyager-world
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-nbd-voyager-world \
            --query "boarding pass passport airport" \
            --orientation landscape \
            --alt "Boarding pass and passport on a travel desk"

      - name: Fetch card-adcb-365-cashback
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-adcb-365-cashback \
            --query "Abu Dhabi corniche modern architecture" \
            --orientation landscape \
            --alt "Abu Dhabi corniche at golden hour"

      - name: Fetch card-adcb-betaqti
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-adcb-betaqti \
            --query "Abu Dhabi skyline evening" \
            --orientation landscape \
            --alt "Abu Dhabi skyline at evening"

      - name: Fetch card-adcb-essential-cashback
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-adcb-essential-cashback \
            --query "Abu Dhabi corniche modern architecture" \
            --orientation landscape \
            --alt "Abu Dhabi corniche at golden hour"

      - name: Fetch card-adcb-lulu-platinum
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-adcb-lulu-platinum \
            --query "supermarket aisle bright shopping cart" \
            --orientation landscape \
            --alt "Grocery aisle in a modern supermarket"

      - name: Fetch card-adcb-lulu-titanium-gold
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-adcb-lulu-titanium-gold \
            --query "supermarket aisle bright shopping cart" \
            --orientation landscape \
            --alt "Grocery aisle in a modern supermarket"

      - name: Fetch card-adcb-shukran
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-adcb-shukran \
            --query "modern shopping mall walkway" \
            --orientation landscape \
            --alt "Modern shopping-mall walkway"

      - name: Fetch card-adcb-talabat
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-adcb-talabat \
            --query "food delivery scooter city street" \
            --orientation landscape \
            --alt "Food-delivery scooter rider in a city street"

      - name: Fetch card-adcb-touchpoints-infinite
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-adcb-touchpoints-infinite \
            --query "Abu Dhabi corniche modern architecture" \
            --orientation landscape \
            --alt "Abu Dhabi corniche at golden hour"

      - name: Fetch card-adcb-touchpoints-platinum
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-adcb-touchpoints-platinum \
            --query "Abu Dhabi corniche modern architecture" \
            --orientation landscape \
            --alt "Abu Dhabi corniche at golden hour"

      - name: Fetch card-adcb-touchpoints-titanium-gold
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-adcb-touchpoints-titanium-gold \
            --query "Abu Dhabi corniche modern architecture" \
            --orientation landscape \
            --alt "Abu Dhabi corniche at golden hour"

      - name: Fetch card-adcb-traveller
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-adcb-traveller \
            --query "boarding pass passport airport" \
            --orientation landscape \
            --alt "Boarding pass and passport on a travel desk"

      - name: Fetch card-emirates-nbd-diners-club
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-nbd-diners-club \
            --query "business dinner restaurant table" \
            --orientation landscape \
            --alt "Business dinner at a modern restaurant"

      - name: Fetch card-emirates-nbd-dnata-platinum
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-nbd-dnata-platinum \
            --query "boarding pass passport airport" \
            --orientation landscape \
            --alt "Boarding pass and passport on a travel desk"

      - name: Fetch card-emirates-nbd-dnata-world
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-nbd-dnata-world \
            --query "boarding pass passport airport" \
            --orientation landscape \
            --alt "Boarding pass and passport on a travel desk"

      - name: Fetch card-emirates-nbd-duo
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-nbd-duo \
            --query "Dubai modern lifestyle cafe" \
            --orientation landscape \
            --alt "Modern Dubai lifestyle scene"

      - name: Fetch card-emirates-nbd-etihad-guest-inspire
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-nbd-etihad-guest-inspire \
            --query "airplane wing sunset clouds" \
            --orientation landscape \
            --alt "Aircraft wing above the clouds at sunset"

      - name: Fetch card-emirates-nbd-go4it-gold
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-nbd-go4it-gold \
            --query "young person mobile payment cafe" \
            --orientation landscape \
            --alt "Young person paying via phone in a café"

      - name: Fetch card-emirates-nbd-go4it-platinum
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-nbd-go4it-platinum \
            --query "young person mobile payment cafe" \
            --orientation landscape \
            --alt "Young person paying via phone in a café"

      - name: Fetch card-emirates-nbd-lulu-247-platinum
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-nbd-lulu-247-platinum \
            --query "supermarket aisle bright shopping cart" \
            --orientation landscape \
            --alt "Grocery aisle in a modern supermarket"

      - name: Fetch card-emirates-nbd-lulu-247-titanium
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-nbd-lulu-247-titanium \
            --query "supermarket aisle bright shopping cart" \
            --orientation landscape \
            --alt "Grocery aisle in a modern supermarket"

      - name: Fetch card-emirates-nbd-marriott-bonvoy-world
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-nbd-marriott-bonvoy-world \
            --query "luxury hotel lobby modern" \
            --orientation landscape \
            --alt "Luxury hotel lobby with warm lighting"

      - name: Fetch card-emirates-nbd-marriott-bonvoy-world-elite
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-nbd-marriott-bonvoy-world-elite \
            --query "luxury hotel lobby modern" \
            --orientation landscape \
            --alt "Luxury hotel lobby with warm lighting"

      - name: Fetch card-emirates-nbd-mastercard-platinum
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-nbd-mastercard-platinum \
            --query "shopping bags receipt cashback" \
            --orientation landscape \
            --alt "Shopping bags and a receipt"

      - name: Fetch card-emirates-nbd-noon-one
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-nbd-noon-one \
            --query "online shopping laptop boxes" \
            --orientation landscape \
            --alt "Online shopping laptop and packed boxes"

      - name: Fetch card-emirates-nbd-share-visa-infinite
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-nbd-share-visa-infinite \
            --query "shopping mall lifestyle people" \
            --orientation landscape \
            --alt "Modern shopping centre with lifestyle shoppers"

      - name: Fetch card-emirates-nbd-share-visa-signature
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-nbd-share-visa-signature \
            --query "shopping mall lifestyle people" \
            --orientation landscape \
            --alt "Modern shopping centre with lifestyle shoppers"

      - name: Fetch card-emirates-nbd-skywards-signature
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-nbd-skywards-signature \
            --query "airplane wing sunset clouds" \
            --orientation landscape \
            --alt "Aircraft wing above the clouds at sunset"

      - name: Fetch card-emirates-nbd-u-by-emaar-family
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-nbd-u-by-emaar-family \
            --query "Dubai apartment interior keys" \
            --orientation landscape \
            --alt "Modern Dubai apartment interior"

      - name: Fetch card-emirates-nbd-u-by-emaar-infinite
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-nbd-u-by-emaar-infinite \
            --query "Dubai apartment interior keys" \
            --orientation landscape \
            --alt "Modern Dubai apartment interior"

      - name: Fetch card-emirates-nbd-u-by-emaar-infinite-emiratis
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-nbd-u-by-emaar-infinite-emiratis \
            --query "Dubai apartment interior keys" \
            --orientation landscape \
            --alt "Modern Dubai apartment interior"

      - name: Fetch card-emirates-nbd-visa-flexi
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-nbd-visa-flexi \
            --query "shopping bags receipt cashback" \
            --orientation landscape \
            --alt "Shopping bags and a receipt"

      - name: Fetch card-emirates-nbd-visa-infinite
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-nbd-visa-infinite \
            --query "Dubai modern lifestyle cafe" \
            --orientation landscape \
            --alt "Modern Dubai lifestyle scene"

      - name: Fetch card-emirates-nbd-visa-platinum
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-nbd-visa-platinum \
            --query "Dubai modern lifestyle cafe" \
            --orientation landscape \
            --alt "Modern Dubai lifestyle scene"

      - name: Fetch card-emirates-nbd-webshopper
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-emirates-nbd-webshopper \
            --query "online shopping checkout laptop" \
            --orientation landscape \
            --alt "Online shopping checkout on a laptop"

      - name: Fetch card-fab-cashback
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-fab-cashback \
            --query "shopping bags receipt cashback" \
            --orientation landscape \
            --alt "Shopping bags and a receipt"

      - name: Fetch card-fab-etihad-guest-infinite
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-fab-etihad-guest-infinite \
            --query "airplane wing sunset clouds" \
            --orientation landscape \
            --alt "Aircraft wing above the clouds at sunset"

      - name: Fetch card-fab-world-elite
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug card-fab-world-elite \
            --query "shopping bags receipt cashback" \
            --orientation landscape \
            --alt "Shopping bags and a receipt"

      - name: Fetch bank-adib
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug bank-adib \
            --query "Abu Dhabi Islamic architecture modern" \
            --orientation landscape \
            --alt "Modern Abu Dhabi Islamic architecture"

      - name: Fetch bank-cbd
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug bank-cbd \
            --query "Dubai office tower facade" \
            --orientation landscape \
            --alt "Dubai office tower glass facade"

      - name: Fetch bank-citi
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug bank-citi \
            --query "global skyscraper international banking" \
            --orientation landscape \
            --alt "International banking skyscraper exterior"

      - name: Fetch bank-dib
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug bank-dib \
            --query "modern Islamic banking architecture geometric" \
            --orientation landscape \
            --alt "Modern Islamic banking architecture"

      - name: Fetch bank-hsbc
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug bank-hsbc \
            --query "international bank entrance glass" \
            --orientation landscape \
            --alt "International bank entrance facade"

      - name: Fetch bank-mashreq
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug bank-mashreq \
            --query "Dubai bank lobby modern interior" \
            --orientation landscape \
            --alt "Modern Dubai bank lobby"

      - name: Fetch bank-standard-chartered
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          npm run fetch:stock -- \
            --slug bank-standard-chartered \
            --query "Asia Africa banking modern building" \
            --orientation landscape \
            --alt "Modern international bank tower"
      - name: Run validation
        run: |
          npm run check
          npm test

      - name: Show manifest summary
        run: |
          echo "Manifest entries:"
          node -e "const m=require('./data/stock/manifest.json'); console.log(m.entries.length+' entries'); m.entries.forEach(e => console.log('  '+e.slug+' ('+e.width+'×'+e.height+') by '+e.photographer));"

      - name: Commit and push seeded images
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/stock/manifest.json public/images/stock/
          if git diff --staged --quiet; then
            echo "No new images to commit."
            exit 0
          fi
          git commit \
            -m "feat(images): Phase E.2 seed — 12 Pexels stock photos" \
            -m "Automated fetch via .github/workflows/seed-images.yml. Stock photographs covering hero-cabin, wallet-hero-cards, programme-skywards, two top-card heroes, and six per-guide heroes. Every <StockImage> slot on the rebuilt homepage, /cards/, /guides/, /news/, and /airlines/ pages now resolves to real photography." \
            -m "Each manifest entry carries full provenance (photographer name + URL, Pexels photo ID, source URL, Pexels licence). Per Charter §'No advertorial-driven recommendations', attribution renders via <ImageCredit> on every consumer page."
          git push origin HEAD:${{ inputs.branch || github.ref_name }}
