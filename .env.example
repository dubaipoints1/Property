# Environment variables — copy to .env (gitignored) and fill in real values.

# Firecrawl API key for the weekly bank scrape pipeline.
# Set to "skip" to dry-run without burning quota.
# See scripts/scrape/_lib.ts.
FIRECRAWL_API_KEY=

# Pexels API key for the stock-image fetcher.
# Get one free at https://www.pexels.com/api/ (200 req/hr).
# Set to "skip" to dry-run without network calls.
# See scripts/images/fetch-stock.ts.
PEXELS_API_KEY=

# fal.ai key for AI illustration (2026-07-29 Charter amendment —
# illustration only; the script refuses documentary subjects).
# Create one at https://fal.ai/dashboard/keys.
# Set to "skip" to dry-run without spending credits.
# See scripts/images/generate-ai.ts.
FAL_KEY=

# Public Buttondown username used by the embedded Friday-brief form.
# Leave blank until the account, confirmation email and privacy settings have
# been verified. The site will show an honest email fallback while blank.
PUBLIC_BUTTONDOWN_USERNAME=

# Cloudflare Web Analytics beacon token (BaseLayout.astro). Optional —
# without it the beacon is omitted and the deploy ships unmeasured.
# Publisher sets this in Cloudflare Pages → Environment Variables.
PUBLIC_CF_BEACON_TOKEN=
